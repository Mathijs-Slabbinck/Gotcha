namespace Gotcha.Core.Enums
{
    public enum StoreItem
    {
        // Game features (one-time)
        AssassinMode,
        ChaosMode,
        TimedKills,
        CustomKillMethods,

        // Lobby size upgrades (one-time)
        Lobby100,
        Lobby150,
        Lobby500,
        Lobby10000
    }
}
